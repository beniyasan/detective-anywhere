FROM python:3.11-slim

WORKDIR /app

# 最小限の依存関係のみ
RUN pip install fastapi==0.104.1 uvicorn==0.24.0 gunicorn==21.2.0

COPY test_app.py .

EXPOSE 8080

CMD ["gunicorn", "test_app:app", "-w", "1", "-k", "uvicorn.workers.UvicornWorker", "--bind", "0.0.0.0:8080"]